spring:
  application:
    name: users
  datasource:
    url: jdbc:postgresql://localhost:5432/theo-users
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 9020
